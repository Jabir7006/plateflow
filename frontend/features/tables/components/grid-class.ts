// One source for the tables grid layout, shared by the grid and its skeleton so
// the loading and loaded states can't drift apart.
export const tablesGridClass = "grid gap-3 grid-cols-2 sm:grid-cols-3 lg:grid-cols-4"
